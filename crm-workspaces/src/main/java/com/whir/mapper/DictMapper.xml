<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.whir.mapper.DictMapper">
    <select id="find" resultType="com.whir.model.Dict" parameterType="com.whir.model.Dict">
        select * from base_dict where dict_type_code = #{id,jdbcType=VARCHAR}
    </select>
</mapper>